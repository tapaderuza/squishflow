# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Kotlin
.kotlin/

# Local configuration (never commit: contains machine-specific SDK paths)
local.properties

# IDE
.idea/
*.iml
*.ipr
*.iws
.fleet/

# Xcode
iosApp/iosApp.xcworkspace/*
!iosApp/iosApp.xcworkspace/contents.xcworkspacedata
xcuserdata/
*.xcuserstate
DerivedData/

# Android
/captures
.externalNativeBuild
.cxx
*.apk
*.aab
*.keystore
!debug.keystore

# Secrets: release signing and store credentials never enter the repository
keystore.properties
*.jks
service-account*.json

# OS
.DS_Store
Thumbs.db

# Local build transcripts
build_check.log

# Raw screen captures: regenerate from the emulator rather than version them
evidence/video/

# Auditionable renders, regenerated by the unit tests
shared/build/audio-preview/
evidence/ios/*.mp4

# Video build intermediates and raw takes (regenerate with submission/video/build.py)
submission/video/work/
submission/video/takes/
submission/video/review/
submission/video/narration/
submission/iphone/*.ipa
